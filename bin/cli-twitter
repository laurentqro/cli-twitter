#!/usr/bin/env bundle exec ruby

require 'readline'
require_relative "../lib/terminal"

controller = TwitterCli::Controller
app = Terminal.new(ARGV, Kernel, controller)


loop { app.interpret(Readline.readline('> ')) }
